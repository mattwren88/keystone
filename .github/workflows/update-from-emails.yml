name: update-from-emails

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GMAIL_LABEL: Keystone College
      EMAIL_DIR: emails
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-api-python-client google-auth google-auth-oauthlib

      - name: Fetch labeled emails
        env:
          GMAIL_CLIENT_ID: ${{ secrets.GMAIL_CLIENT_ID }}
          GMAIL_CLIENT_SECRET: ${{ secrets.GMAIL_CLIENT_SECRET }}
          GMAIL_REFRESH_TOKEN: ${{ secrets.GMAIL_REFRESH_TOKEN }}
        run: |
          python scripts/fetch_gmail_emails.py

      - name: Update site content
        run: |
          python scripts/update_site.py

      - name: Commit changes
        run: |
          if [ -n "$(git status --porcelain index.html)" ]; then
            git config user.name "keystone-bot"
            git config user.email "keystone-bot@users.noreply.github.com"
            git add index.html
            git commit -m "Update from emails"
            git push
          else
            echo "No changes to commit."
          fi
